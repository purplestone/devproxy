#安装目录有空格

#readme写上界面的url

#apache配置说明

#http:// 自动去掉

#代理我本地某个固定目录下的JS与CSS文件。这个你已经实现了，只是使用起来感觉弯弯绕有点多。考虑到用户的懒惰性，最优的方案是：提供配置界面—》开启，即可，其他操作尽可能封装起来。

#考虑到我刚才想到的模拟返回值，可否简单的在其中实现一个界面：输入线上的接口地址，然后输入一个JSON，启动后，让这个接口固定返回某个JSON或者其他自己设定的返回值。

================ 0.7 =================

请求的host可以选择使用src的host, 可以使用自定义host（增加ip nds功能）

点击本地文件链接后的ico图片用编辑器打开

切换或勾选时如果返回正确才生效

当有修改配置文件时提示重置代理,将按钮文案改为：（配置文件有更新）

本地文件增加正则转换功能

可增加自定义环境

后执行的特殊规则表


================ 0.8 ================

